spring:
  application:
    name:
      job-radar
  profiles:
    default: local
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
